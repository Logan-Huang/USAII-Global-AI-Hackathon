<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext HTTP by default since API 28. The dev backend is a plain-HTTP
  loopback server, reached from the Android emulator at 10.0.2.2 (the host's 127.0.0.1).
  We permit cleartext ONLY to those loopback/dev hosts; everything else (e.g. a deployed
  HTTPS backend) stays TLS-only. OSM tile/Overpass traffic from osmdroid is HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
